name: localshare_mobile
description: "LocalShare - Offline file sharing mobile app"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.0
  qr_flutter: ^4.1.0
  socket_io_client: any
  sqflite: ^2.3.0
  permission_handler: any
  path_provider: ^2.1.1
  intl: ^0.19.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
